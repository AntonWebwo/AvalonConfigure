# AvalonConfigure

**AvalonConfigure** — это консольное приложение на C++ для настройки майнинг-устройств серии Avalon через HTTP-запросы к веб-интерфейсу устройства. Программа позволяет отправлять конфигурационные параметры (пулы, воркеры, режим работы и т. д.) на устройство по указанному IP-адресу с использованием авторизации.

## 📦 Возможности

- Настройка до трёх пулов майнинга с логинами и паролями.
- Поддержка различных режимов работы устройства.
- Передача дополнительных опций (`--moreoption`).
- Автоматическое кодирование данных для передачи через HTTP.
- Обработка ответа от устройства и вывод результата в формате JSON.

---

## 🔧 Установка

### Требования

- Windows (поддержка WinHTTP)
- Компилятор с поддержкой C++11 или выше (например, MSVC)

### Сборка

```bash
cl /EHsc AvalonConfigure.cpp
```

---

## 🚀 Использование

```bash
AvalonConfigure.exe [опции]
```

### Пример запуска:

```bash
AvalonConfigure.exe \
  --url 192.168.1.100 \
  --user root \
  --password root \
  --pool1 stratum+tcp://pool.example.com:3333 \
  --worker1 worker1 \
  --passwd1 pass123 \
  --pool2 stratum+tcp://backup.pool.com:4444 \
  --worker2 worker2 \
  --passwd2 pass456 \
  --pool3 stratum+tcp://third.pool.com:5555 \
  --worker3 worker3 \
  --passwd3 pass789 \
  --mode mining \
  --moreoption extra_data
```

### Все опции:

| Опция              | Описание                         | По умолчанию |
|--------------------|----------------------------------|--------------|
| `--url`            | IP-адрес устройства *(обязательно)* | —            |
| `--user`           | Логин для доступа                | `root`       |
| `--password`       | Пароль                           | `root`       |
| `--pool1`          | Адрес первого пула               | —            |
| `--worker1`        | Имя воркера                      | —            |
| `--passwd1`        | Пароль воркера                   | —            |
| `--pool2`, `worker2`, `passwd2` | Аналогично для второго пула     | —            |
| `--pool3`, `worker3`, `passwd3` | Аналогично для третьего пула    | —            |
| `--mode`           | Режим работы устройства          | —            |
| `--moreoption`     | Дополнительные опции             | —            |

---

## 📄 Пример вывода

```json
{
  "status": "success",
  "http_code": 200,
  "message": "Configuration request sent to 192.168.1.100",
  "credentials": {
    "user": "root",
    "password": "root"
  },
  "request": {
    "pool1": "stratum+tcp://pool.example.com:3333",
    "worker1": "worker1",
    "passwd1": "pass123",
    ...
  }
}
```

---

## ⚠️ Ограничения

- Только для Windows.
- Таймаут соединения установлен в **1 секунду**.
- Не обрабатывает редиректы и сложные HTTP-ответы.

---

## 👨‍💻 Автор

**Anton Vinogradov**  
Telegram: [@vinantole](https://t.me/vinantole)  
© 2026

---

## 📜 Лицензия

MIT License (см. файл `LICENSE`)
